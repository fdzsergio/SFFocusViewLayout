# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage
osx_image: xcode7.1

language: objective-c
#before_install:
#- gem install cocoapods --no-rdoc --no-ri --no-document --quiet # Since Travis is not always on latest version
#install:
#- gem install xcpretty --no-rdoc --no-ri --no-document --quiet
script:
- set -o pipefail && xcodebuild analyze test -workspace SFFocusViewLayout.xcworkspace -scheme SFFocusViewLayout -sdk iphonesimulator | xcpretty -c
- pod lib lint --quick

after_success:
- bash <(curl -s https://codecov.io/bash)
